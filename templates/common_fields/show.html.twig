{% extends 'base.html.twig' %}

{% block title %}CommonFields{% endblock %}

{% block body %}
    <h1>CommonFields</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ common_field.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ common_field.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ common_field.image }}</td>
            </tr>
            <tr>
                <th>Contacts</th>
                <td>{{ common_field.contacts }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('common_fields_index') }}">back to list</a>

    <a href="{{ path('common_fields_edit', {'id': common_field.id}) }}">edit</a>

    {{ include('common_fields/_delete_form.html.twig') }}
{% endblock %}
